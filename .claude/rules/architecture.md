---
paths:
  - "src/**"
---

## Architecture notes

<!--
  Loads only when Claude touches files under src/. Good place for: module boundaries that
  aren't enforced by tooling, "don't import X from Y" rules, data-flow gotchas. Delete this
  comment once filled in, or delete this file entirely if not useful for this repo.
-->
