/// <reference types="vite/client" />

// Injected by vite.config.ts's `define` at build time -- the short commit
// SHA the running build was built from, for the "App version" line in
// PersonMenu.tsx.
declare const __APP_COMMIT_SHA__: string;
